#include "../../include/kernel.h"

/*
 *=======================================================================================
 *kernel_send関数
 *カーネル空間のプロセスにシグナルを送信する関数
 *=======================================================================================
 */
void kernel_send(struct Process *proc, u32_t msg){
	queue_push(proc->irq, msg);
}
